spring:
  application:
    name: department-service
  datasource:
    url: jdbc:mysql://localhost:3306/department_db?createDatabaseIfNotExist=true
    username: root
    password: 
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update